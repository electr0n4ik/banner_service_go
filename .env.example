# Application
GIN_MODE=release
PORT=8080

# Database (если потребуется)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=myapp

# JWT Secret (если потребуется)
JWT_SECRET=your-secret-key

# Other settings
LOG_LEVEL=info